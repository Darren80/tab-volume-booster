1. Never shorten the names of variables, always write them out in full.

2. Always parameterize, no magic variables. add all parameters to a variable called "SETTINGS" or "[file]SETTINGS" at the top of the file.
-   No need to parameterise variables which do not change.